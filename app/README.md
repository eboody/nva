# `app`

Operator translation: `app` is where pet-resort work becomes a reviewable packet, draft, ranking, or tool request. It helps staff and managers move faster while keeping customer sends, provider/PMS writes, payment movement, schedule changes, care/safety decisions, and policy exceptions behind explicit review.

`app` is the application/use-case layer for the pet-resort agent platform. It composes [semantic](../docs/glossary-architecture-terms.md#semantic) business meaning from [`domain`](../domain/README.md) into workflow packets, draft-only automation decisions, agent prompt payloads, and tool-port [contracts](../docs/glossary-architecture-terms.md#contract) (source-backed code promises, not legal/customer contracts) that API, worker, and CLI shells can execute.

Start at [`src/lib.rs`](./src/lib.rs). The crate root exposes workflow modules for [`agents`](./src/agents.rs), [`booking_triage`](./src/booking_triage.rs), [`checkout_completion`](./src/checkout_completion.rs), [`crm_retention`](./src/crm_retention.rs), [`daily_update`](./src/daily_update.rs), [`local_smoke`](./src/local_smoke.rs), [`manager_daily_brief`](./src/manager_daily_brief.rs), and [`tools`](./src/tools.rs). The [`prelude`](./src/lib.rs) intentionally stays small: it re-exports common agent packet/spec helpers and a few tool-port request modules, not every workflow type.

This crate does not own domain truth. It may define application request packets, evaluation packets, draft records, audit-event drafts, and port contracts, but the business language and invariants come from `domain::*` types such as `domain::entities::Reservation`, `domain::policy::ReviewGate`, `domain::workflow::Event`, `domain::source::Provenance`, `domain::analytics::service_demand::Fact`, and service-line policy types documented in [`../domain`](../domain/README.md). When `app` has local enums such as `booking_triage::ReadinessBucket` or `checkout_completion::CompletionStatus`, those are use-case classifications that eventually map back to domain statuses, review gates, source refs, audit records, or blocked actions; they are not alternate canonical domain models.

## README vs Rustdoc contract

This README is the application workflow wiki: use it to find use-case packets, draft/review boundaries, tool ports, and the source modules that compose `domain` truth into labor-saving review queues. Keep it navigational and avoid stale duplicate Rust snippets.

Executable app examples belong in Rustdoc on [`src/lib.rs`](./src/lib.rs), workflow modules such as [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs), and tool-port modules such as [`src/tools.rs`](./src/tools.rs). Those examples should compile under `cargo test -p app --doc` and show source-grounded context packets, draft validation, blocked actions, review gates, and outcome capture without implying live provider/customer side effects.

## Workflow glossary links

When this README or generated Rustdocs use app-layer terms, link nearby operator explanations instead of making non-coders infer the boundary from Rust names alone:

- [Workflow packet](../docs/glossary-workflow-state-terms.md#workflow-packet): the review bundle a shell, staff member, manager, or agent runner works from.
- [Draft](../docs/glossary-workflow-state-terms.md#draft): prepared message/task/evidence/update work that has not been sent, applied, or approved.
- [Review gate](../docs/glossary-workflow-state-terms.md#review-gate): the named human-approval stop for manager, medical, behavior, customer-message, or refund/deposit-sensitive work.
- [Blocked action](../docs/glossary-workflow-state-terms.md#blocked-action): live customer/provider/payment/schedule/policy actions the workflow must not take directly.
- [Outcome capture](../docs/glossary-workflow-state-terms.md#outcome-capture): the evidence record of what staff did and what labor result was observed.
- [Agent spec](../docs/glossary-workflow-state-terms.md#agent-spec): the operating contract that names an agent helper's purpose, allowed tools, forbidden actions, and default review gates.
- [Tool port](../docs/glossary-architecture-terms.md#tool-port-apptools): the typed capability interface app workflows use for reads, checks, drafts, and future adapter implementations.
- [Source refs and provenance](../docs/glossary-source-data-terms.md#domainsourceprovenance-and-domainsourcerecordref-as-data-evidence): the evidence trail behind workflow facts.

## Module navigation

### Crate surface and shared agent contracts

- [`src/lib.rs`](./src/lib.rs) is the crate registry. It declares each workflow module and the narrow `app::prelude` re-export surface.
- [`src/agents.rs`](./src/agents.rs) defines the common `app::agents::WorkflowAgent<Input, Output>` trait, `AgentPromptPacket<T>`, `AgentSpec`, and `baseline_agent_specs()`. It builds executable prompt/spec vocabulary from `domain::agent` and `domain::workflow`, while preserving safety rails such as `domain::policy::ReviewGate`.
- [`src/tools.rs`](./src/tools.rs) defines tool-port traits and request/response vocabulary for customer stores, reservation systems, agent runtimes, portal lookup, payment gateways, message drafting, document/OCR intake, media capture, and Hermes automation hooks. [`src/tools/error.rs`](./src/tools/error.rs) owns the shared `tools::Error`, `Resource`, `ResourceId`, and `ExternalFailure` values used by those ports.

### Workflow modules

- [`src/booking_triage.rs`](./src/booking_triage.rs) models the booking-triage use case. It has a typestate request (`booking_triage::Request<Intake>` through `Request<ReadyForPolicyDecision>`), deterministic rule evaluations, AI recommendation and confirmation draft packets, staff evaluation packets, safe/blocked action enums, and a repository-backed `booking_triage::Service<R>` that reads `domain::entities::Reservation` before producing a `StaffEvaluationPacket`.
- [`src/checkout_completion.rs`](./src/checkout_completion.rs) evaluates whether a source-observed checkout plus staff handoff can become a checkout-completion packet. It composes `domain::source::Provenance`, `domain::source::reservation::Status`, `domain::entities::reservation::Status`, and `domain::policy::ReviewGate` into `checkout_completion::Packet` without mutating provider or PMS state.
- [`src/crm_retention.rs`](./src/crm_retention.rs) evaluates post-stay follow-up eligibility. It consumes a `checkout_completion::Packet`, source-grounded opportunities, contact permission evidence, `domain::message::Channel`, and `domain::source::RecordRef` values before producing draft-only retention packets and outcome records.
- [`src/daily_update.rs`](./src/daily_update.rs) builds the daily-care-update MVP preview. It validates `domain::workflow::Event` and allowed actions, turns `domain::entities::CareNote` facts into a `daily_care_update::Input`/`Output`, creates a customer-safe draft message, approval record, send stub, and audit log, and implements `WorkflowAgent` for `daily_care_update::Agent`.
- [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs) ranks manager actions from source-grounded service-demand facts, checkout exceptions, and retention opportunities. It carries labor-minute estimates and removed-manual-work categories so a manager sees a prioritized brief instead of reconciling dashboards by hand.
- [`src/local_smoke.rs`](./src/local_smoke.rs) runs a local full-chain fixture through inquiry/profile/vaccine evidence, booking triage, confirmation draft, check-in view, daily update preview, checkout completion, and retention follow-up. It is a read-only demonstration of the workflow boundaries and review gates, not live provider automation.

## What `app` owns

`app` owns use-case composition and executable workflow envelopes:

1. Request packets that gather the already-normalized inputs a workflow needs, such as `checkout_completion::Request`, `crm_retention::Request`, `daily_update::MvpPreviewRequest`, and `manager_daily_brief::Request`.
2. Evaluation/result packets that expose recommended statuses, review gates, blocked actions, safe draft actions, audit-event drafts, and source evidence for shells and reviewers. Examples include `booking_triage::StaffEvaluationPacket`, `checkout_completion::Packet`, `crm_retention::Packet`, `daily_update::MvpPreview`, and `manager_daily_brief::Packet`.
3. Draft-only artifacts such as `booking_triage::ConfirmationDraft`, `daily_update::CustomerMessageDraft`, `daily_update::SendStub`, `checkout_completion::AuditEventDraft`, `crm_retention::OutcomeRecord`, and `manager_daily_brief::OutcomeRecord`.
4. Tool-port traits and boundary request/response types in [`src/tools.rs`](./src/tools.rs). These describe what application workflows need from stores, portals, payment gateways, messaging, document/OCR systems, cameras, and Hermes automation hooks without binding the use-case layer to one provider.
5. Lightweight orchestration services, such as `booking_triage::Service<R>`, that compose repositories and deterministic workflow functions.

`app` does not define provider DTOs, storage records, database schemas, HTTP routes, live Gingr transport, payment capture behavior, or customer-message delivery. Those belong to [`integrations/gingr`](../integrations/gingr/README.md), [`storage`](../storage/README.md), and shell crates under [`../apps`](../apps) as applicable.

## Type/module map

| Concept | Public type/module path | Defined in | Relationship |
| --- | --- | --- | --- |
| Crate module registry | `app` crate root | [`src/lib.rs`](./src/lib.rs) | Exposes workflow modules and small `app::prelude`. |
| Agent workflow trait | `app::agents::WorkflowAgent<Input, Output>` | [`src/agents.rs`](./src/agents.rs) | Uses `domain::workflow::Event` and `domain::workflow::Result<T>` to build/validate prompt packets. |
| Agent prompt packet | `app::agents::AgentPromptPacket<T>` | [`src/agents.rs`](./src/agents.rs) | Composes `domain::agent::Name`, `Purpose`, `PolicyInstruction`, `OutputSchemaName`, and `domain::workflow::Event` with workflow input. |
| Baseline specs | `app::agents::baseline_agent_specs`, `app::agents::AgentSpec` | [`src/agents.rs`](./src/agents.rs) | Produces `domain::agent::Spec` values with allowed tools, forbidden actions, and `domain::policy::ReviewGate` defaults. |
| Booking typestate request | `app::booking_triage::Request<S>`, `RequestState`, `Intake`, `PetProfileAttached`, `PolicyAttached`, `ReadyForPolicyDecision` | [`src/booking_triage.rs`](./src/booking_triage.rs) | Application staging around `domain::pet::Name` and policy-snapshot evidence; does not replace reservation truth. |
| Booking request/support values | `booking_triage::Reservation`, `PolicySnapshot`, `PetProfile`, `PolicyAttachedData` | [`src/booking_triage.rs`](./src/booking_triage.rs) | Validated app labels/snapshots used to build a triage packet. |
| Booking rule ids/evaluations | `booking_triage::rule::{Id, Decision, ReviewFinding, Evaluation}` | [`src/booking_triage.rs`](./src/booking_triage.rs) | Deterministic rule review surface with evidence refs, failure codes, readiness buckets, review gates, and safe actions. |
| Booking readiness/gates/actions | `booking_triage::{ReadinessBucket, ApprovalGate, FailureCode, SafeAgentAction, BlockedAction, StaffDecisionBoundary}` | [`src/booking_triage.rs`](./src/booking_triage.rs) | App-level triage classifications that preserve review and blocked-action boundaries. |
| Booking deterministic result | `booking_triage::DeterministicResult` | [`src/booking_triage.rs`](./src/booking_triage.rs) | Aggregates rule evaluations into recommended readiness, approval gates, and blocked actions. |
| Booking AI/draft/audit packet | `booking_triage::{AiRecommendation, AgentRecommendedAction, ConfirmationDraft, AuditEventDraft, StaffEvaluationPacket}` | [`src/booking_triage.rs`](./src/booking_triage.rs) | Draft-only staff packet; maps suggested status back to `domain::entities::reservation::Status`. |
| Booking repository/service | `booking_triage::reservation::Repository`, `booking_triage::Service<R>`, `booking_triage::Error`, `AppResult<T>` | [`src/booking_triage.rs`](./src/booking_triage.rs) | Reads `domain::entities::Reservation` through a narrow app repository and evaluates hard stops/deposits. |
| Checkout handoff request | `checkout_completion::{CareSummary, StaffHandoff, Request}` | [`src/checkout_completion.rs`](./src/checkout_completion.rs) | Composes `domain::entities::ActorRef`, `domain::source::Provenance`, and `domain::source::reservation::Status`. |
| Checkout evaluation | `checkout_completion::{CompletionStatus, Packet, Workflow}` | [`src/checkout_completion.rs`](./src/checkout_completion.rs) | Suggests `domain::entities::reservation::Status::CheckedOut` only when source status and staff handoff agree. |
| Checkout draft controls | `checkout_completion::{BelongingsStatus, DepartureNotesReview, SafeAgentAction, BlockedAction, AuditEventDraft}` | [`src/checkout_completion.rs`](./src/checkout_completion.rs) | Keeps customer messages, provider/PMS mutations, and payment actions blocked while producing reviewable evidence. |
| CRM opportunity evidence | `crm_retention::{OpportunityEvidence, RetentionOpportunity, SourceGroundedReasonCode, OpportunityKind}` | [`src/crm_retention.rs`](./src/crm_retention.rs) | Source-grounded retention candidates with `domain::source::Provenance`. |
| CRM contact permission | `crm_retention::{ContactPermission, ConsentStatus}` | [`src/crm_retention.rs`](./src/crm_retention.rs) | Uses `domain::message::Channel` and `domain::source::RecordRef` before any draft channel is allowed. |
| CRM request/evaluation packet | `crm_retention::{Request, StaffReviewPacket, Packet, Workflow}` | [`src/crm_retention.rs`](./src/crm_retention.rs) | Consumes `checkout_completion::Packet`, customer/reservation ids, contact permission, and opportunities. |
| CRM eligibility/actions/outcomes | `crm_retention::{FollowUpEligibility, EligibilityReason, IneligibilityReason, SafeAgentAction, BlockedAction, FollowUpOutcome, OutcomeRecord}` | [`src/crm_retention.rs`](./src/crm_retention.rs) | Draft-only retention follow-up and outcome evidence; blocks live messages, discounts, payment moves, and provider mutation. |
| Daily update request/preview | `daily_update::{MvpPreviewRequest, MvpPreview, build_mvp_preview}` | [`src/daily_update.rs`](./src/daily_update.rs) | Validates workflow event and staff notes, then composes prompt packet, output, message draft, approval, send stub, and audit log. |
| Daily care agent IO | `daily_update::daily_care_update::{Input, Output, Agent}` | [`src/daily_update.rs`](./src/daily_update.rs) | Agent-specific input/output used by `app::agents::WorkflowAgent`. |
| Daily draft/review types | `daily_update::{CustomerMessageDraft, ReviewDisposition, InternalFlag, IncludedFact, OmittedFact, SendStub}` | [`src/daily_update.rs`](./src/daily_update.rs) | Customer-facing draft, internal flags, redactions, approval-required send stub, and source-note traceability. |
| Daily scalar enums/newtypes | `daily_update::{Audience, LanguageTag, ToneLabel, RedactionProfile, ReviewReason, FlagMessage, FactSummary, InternalFlagCode, InternalFlagSeverity, RecommendedFlagAction, OmissionReason, SendMode}` | [`src/daily_update.rs`](./src/daily_update.rs) | App-specific output vocabulary around customer-safe update drafting. |
| Local smoke fixture | `local_smoke::{run_fixture, FullChainEvidence, Stage, SmokeBoundaries}` | [`src/local_smoke.rs`](./src/local_smoke.rs) | Demonstrates the full draft-only workflow chain with local semantic fixtures. |
| Local smoke evidence wrappers | `local_smoke::{SourceEventKey, ReviewEvidenceRef, SmokeConfirmationDraft, TodayView, CheckoutCompletion, RetentionFollowUp}` | [`src/local_smoke.rs`](./src/local_smoke.rs) | Read-only wrappers exposing stage evidence and review gates for tests/demos. |
| Manager brief request | `manager_daily_brief::{Request, ScopedCheckoutPacket, ScopedRetentionPacket, ManagerBriefPersona}` | [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs) | Scopes service-demand facts, checkout packets, and retention packets to location/operating day/persona. |
| Manager brief action packet | `manager_daily_brief::{Packet, BriefAction, SourceFact, LaborImpactEstimate}` | [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs) | Source-grounded, prioritized action list with labor-minute estimates. |
| Manager brief classifications | `manager_daily_brief::{BriefActionKind, BriefActionPriority, RemovedManualWork, SourceFactKind, SafeAgentAction, BlockedAction, FeedbackOutcome}` | [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs) | Names the removed manual work, safe draft actions, blocked live actions, and feedback outcomes. |
| Manager brief scalars/errors | `manager_daily_brief::{BriefSummary, ActionId, ActionRationale, LaborMinutes, AggregateLaborMinutes, DemandThresholdUnits, Error, Result}` | [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs) | Validated application scalars used by manager brief packets and outcome records. |
| Manager feedback record | `manager_daily_brief::OutcomeRecord` | [`src/manager_daily_brief.rs`](./src/manager_daily_brief.rs) | Records action feedback and actual labor minutes without external mutation. |
| Tool store/runtime traits | `tools::{CustomerStore, ReservationSystem, AgentRuntime}` | [`src/tools.rs`](./src/tools.rs) | App-facing ports for domain aggregate reads, reservation availability/draft updates, and structured agent execution. |
| Tool availability/draft update | `tools::availability::{Request, Outcome, Decision}`, `tools::draft_update::{Request, Rationale, draft::Id}` | [`src/tools.rs`](./src/tools.rs) | Port request/response vocabulary around availability checks and draft reservation updates. |
| Tool portal/payment/messaging/document/media/Hermes ports | `tools::{portal, payment, messaging, documents, media, hermes}` | [`src/tools.rs`](./src/tools.rs) | Boundary modules for external services; implementations belong outside this crate. |
| Tool errors | `tools::{Error, Resource, ResourceId, ExternalFailure, Result}` | [`src/tools/error.rs`](./src/tools/error.rs) | Shared app-port failures, including policy denial from `domain::policy::denial::Reason`. |
| External tool inventory | `tools::ExternalToolCandidate` | [`src/tools.rs`](./src/tools.rs) | Names candidate external systems such as Gingr, payment, messaging, file storage, OCR, camera, Hermes, and Postgres. |

## Request/evaluation/draft/audit/action families

The workflow modules use repeated type families so maintainers can navigate without guessing:

- `Request` types gather normalized inputs: [`booking_triage::Request<S>`](./src/booking_triage.rs), [`checkout_completion::Request`](./src/checkout_completion.rs), [`crm_retention::Request`](./src/crm_retention.rs), [`daily_update::MvpPreviewRequest`](./src/daily_update.rs), [`manager_daily_brief::Request`](./src/manager_daily_brief.rs), and the port-level `tools::*::Request` types in [`src/tools.rs`](./src/tools.rs).
- Evaluation/result types explain what the workflow concluded: [`booking_triage::DeterministicResult`](./src/booking_triage.rs), [`booking_triage::rule::Evaluation`](./src/booking_triage.rs), [`checkout_completion::CompletionStatus`](./src/checkout_completion.rs), [`crm_retention::FollowUpEligibility`](./src/crm_retention.rs), [`daily_update::ReviewDisposition`](./src/daily_update.rs), and [`manager_daily_brief::BriefAction`](./src/manager_daily_brief.rs).
- Packet types are the shell-facing outputs: [`booking_triage::StaffEvaluationPacket`](./src/booking_triage.rs), [`checkout_completion::Packet`](./src/checkout_completion.rs), [`crm_retention::Packet`](./src/crm_retention.rs), [`daily_update::MvpPreview`](./src/daily_update.rs), [`manager_daily_brief::Packet`](./src/manager_daily_brief.rs), and [`agents::AgentPromptPacket<T>`](./src/agents.rs).
- Draft types are safe artifacts awaiting review, not live actions: [`booking_triage::ConfirmationDraft`](./src/booking_triage.rs), [`daily_update::CustomerMessageDraft`](./src/daily_update.rs), [`daily_update::SendStub`](./src/daily_update.rs), [`crm_retention::OutcomeRecord`](./src/crm_retention.rs), [`manager_daily_brief::OutcomeRecord`](./src/manager_daily_brief.rs), and port draft shapes under [`tools::draft_update`](./src/tools.rs), [`tools::messaging::draft`](./src/tools.rs), and [`tools::hermes`](./src/tools.rs).
- Audit/action enums keep review boundaries explicit: [`booking_triage::AuditEventDraft`](./src/booking_triage.rs), [`checkout_completion::AuditEventDraft`](./src/checkout_completion.rs), [`booking_triage::SafeAgentAction`](./src/booking_triage.rs), [`checkout_completion::BlockedAction`](./src/checkout_completion.rs), [`crm_retention::SafeAgentAction`](./src/crm_retention.rs), [`manager_daily_brief::RemovedManualWork`](./src/manager_daily_brief.rs), and `domain::entities::audit::Action` used in [`daily_update.rs`](./src/daily_update.rs).

## How app types compose `domain` without owning truth

`app` should be read as a composition layer over `domain`:

1. Identity and aggregate facts come from `domain::entities`. `booking_triage::Service<R>` reads `domain::entities::Reservation` through `booking_triage::reservation::Repository`; checkout, retention, daily-update, and manager-brief requests carry `domain::entities::*Id`, `domain::entities::ActorRef`, care notes, approval records, messages, or audit subjects instead of inventing new identity systems.
2. Review and automation boundaries come from `domain::policy` and `domain::workflow`. Application modules may define local safe/blocked actions, but customer sends, manager approval, medical document review, payment review, and draft-only automation are expressed through `domain::policy::ReviewGate`, `domain::policy::automation::Level`, `domain::workflow::AllowedAction`, and `domain::workflow::Result<T>`.
3. Source evidence comes from `domain::source` and `domain::analytics`. Checkout and CRM packets preserve `domain::source::Provenance` and `RecordRef`; manager briefs consume `domain::analytics::service_demand::Fact`; local smoke uses `domain::source::System::Gingr` only as source evidence.
4. App status suggestions map back to domain lifecycle values rather than replacing them. For example, `booking_triage::StaffEvaluationPacket::suggested_status()` returns `domain::entities::reservation::Status`, and `checkout_completion::Packet::suggested_reservation_status()` returns an optional `domain::entities::reservation::Status`.
5. App-local newtypes such as `booking_triage::EvidenceRef`, `checkout_completion::CareSummary`, `crm_retention::EvidenceSummary`, `daily_update::FactSummary`, and `manager_daily_brief::BriefSummary` are workflow packet fields. They summarize evidence for review; they do not become canonical customer, pet, reservation, policy, or provider records.

## Labor-cost and automation role

`app` reduces labor cost by assembling already-normalized facts into reviewable decisions and draft artifacts:

1. [`booking_triage`](./src/booking_triage.rs) turns reservation hard stops, deposit status, evidence refs, and policy gates into a staff evaluation packet so staff see why a request is ready, missing information, blocked, or awaiting human approval.
2. [`checkout_completion`](./src/checkout_completion.rs) compares source checkout status and staff handoff completeness, reducing manual checkout audits while keeping provider/PMS mutations and customer messages blocked.
3. [`crm_retention`](./src/crm_retention.rs) filters follow-up opportunities through checkout verification, contact consent, allowed channels, and source evidence so staff review only source-grounded retention candidates.
4. [`daily_update`](./src/daily_update.rs) transforms staff notes into customer-safe draft previews with omitted-fact and internal-flag evidence, reducing message-writing handoffs while preserving approval gates.
5. [`manager_daily_brief`](./src/manager_daily_brief.rs) ranks service-demand, checkout-exception, and retention actions with labor-minute estimates, so managers start from a prioritized review queue instead of reconciling dashboards manually.
6. [`tools`](./src/tools.rs) keeps external side effects behind ports. Workflow logic can produce draft/update requests and blocked actions without directly sending messages, mutating Gingr, moving payments, or editing schedules.

### Reservations and checkout operator summary

Booking and checkout surfaces are staff queues, not autonomous operators. [`booking_triage`](./src/booking_triage.rs) helps front desk and managers decide whether a reservation is ready, missing information, vaccine pending, special-review, waitlisted, or failed-safe. [`checkout_completion`](./src/checkout_completion.rs) helps departure staff decide whether checkout evidence is complete, needs handoff review, or conflicts with source provider status. Together they reduce labor by turning scattered source facts into one review packet, one blocked-action list, and one draft/audit handoff instead of asking staff to reconcile provider records, care notes, payment signals, and customer-message copy by hand.

Neither workflow may perform live booking confirmation/rejection, check-in/out, provider/PMS mutation, capacity release, customer-message sending, payment collection, refund, waiver, discount, or closeout execution. Source authority stays with provider/PMS lifecycle records, approved location policy, verified vaccine/document and care facts, trusted payment/deposit/ledger records, staff-submitted handoff evidence, and accountable human approvals. Review gates protect pets, customers, and staff whenever evidence is missing, stale, conflicting, sensitive, payment-related, or tied to manager/care/medical/behavior/customer-message approval.

## Cross-crate relationships

- [`domain`](../domain/README.md) owns semantic truth. `app` composes `domain::agent`, `domain::analytics`, `domain::audit`, `domain::customer`, `domain::document`, `domain::entities`, `domain::message`, `domain::money`, `domain::operations`, `domain::payment`, `domain::pet`, `domain::policy`, `domain::source`, `domain::vaccine`, and `domain::workflow` values from files such as [`../domain/src/entities.rs`](../domain/src/entities.rs), [`../domain/src/workflow.rs`](../domain/src/workflow.rs), [`../domain/src/source.rs`](../domain/src/source.rs), [`../domain/src/agent.rs`](../domain/src/agent.rs), [`../domain/src/policy.rs`](../domain/src/policy.rs), and [`../domain/src/analytics.rs`](../domain/src/analytics.rs).
- [`storage`](../storage/README.md) owns persisted records and projections. App workflows should consume semantic domain values or storage-normalized projections rather than redefining storage code tables; manager daily brief outcomes have a storage projection in [`../storage/src/operations.rs`](../storage/src/operations.rs).
- [`integrations/gingr`](../integrations/gingr/README.md) owns provider DTOs, endpoint builders, response/webhook parsing, and mapping. App workflows may see Gingr through `domain::source::Provenance` or app tool ports, but raw Gingr payload handling belongs in files such as [`../integrations/gingr/src/endpoint/mod.rs`](../integrations/gingr/src/endpoint/mod.rs), [`../integrations/gingr/src/response.rs`](../integrations/gingr/src/response.rs), and [`../integrations/gingr/src/mapping/mod.rs`](../integrations/gingr/src/mapping/mod.rs).
- Shell crates under [`../apps`](../apps) expose or run these workflows. The API shell lives in [`../apps/api/src/http.rs`](../apps/api/src/http.rs) and [`../apps/api/src/main.rs`](../apps/api/src/main.rs); the worker shell lives in [`../apps/worker/src/runtime.rs`](../apps/worker/src/runtime.rs) and [`../apps/worker/src/main.rs`](../apps/worker/src/main.rs); the CLI shell lives in [`../apps/cli/src/main.rs`](../apps/cli/src/main.rs).
- App workflow contracts are tested under [`tests`](./tests), including [`tests/booking_triage_mvp.rs`](./tests/booking_triage_mvp.rs), [`tests/daily_care_update_mvp.rs`](./tests/daily_care_update_mvp.rs), [`tests/checkout_completion_workflow_contracts.rs`](./tests/checkout_completion_workflow_contracts.rs), [`tests/crm_retention_workflow_contracts.rs`](./tests/crm_retention_workflow_contracts.rs), [`tests/manager_daily_brief_workflow_contracts.rs`](./tests/manager_daily_brief_workflow_contracts.rs), [`tests/full_chain_local_smoke.rs`](./tests/full_chain_local_smoke.rs), and [`tests/workflow_service_composition_contracts.rs`](./tests/workflow_service_composition_contracts.rs).

## Maintainer notes

- Preserve semantic module paths in prose and code. Prefer `app::booking_triage::StaffEvaluationPacket`, `domain::entities::reservation::Status`, `domain::policy::ReviewGate`, and `domain::source::Provenance` over flattened names when the boundary matters.
- Add workflow types here when they describe a use-case packet, decision, draft artifact, port request, or orchestration service. Add domain concepts to [`../domain`](../domain/README.md), provider facts to [`../integrations/gingr`](../integrations/gingr/README.md), and persistence shapes to [`../storage`](../storage/README.md).
- Keep draft/live boundaries visible. If a workflow can only draft or summarize, expose that through `SafeAgentAction`, `BlockedAction`, `SendStub`, `domain::policy::ReviewGate`, or a port result instead of hiding it in comments.
- Keep source evidence attached. Workflows that depend on provider or projection facts should preserve `domain::source::Provenance`, `domain::source::RecordRef`, data-quality status, or audit refs so reviewers can trace why an action was suggested.
- Do not claim a live side effect unless a shell or integration implements it. This crate describes and evaluates application workflows; external mutation belongs behind tool ports and provider adapters.
