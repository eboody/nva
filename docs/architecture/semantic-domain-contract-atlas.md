# Semantic domain contract atlas

Status: canonical architecture inventory for the semantic-domain-hardening board.

Purpose: this atlas names the proposed single owner for every important NVA pilot concept family so later refactors can be reviewed mechanically instead of by taste. It is evidence-based from `domain/src/**/*.rs`, existing strategic tests, app/storage/integration consumers, `README.md`, `domain/README.md`, `app/README.md`, `storage/README.md`, `docs/pilot/safety-boundaries.md`, `docs/internal/archive/architecture/builder-modernization-policy.md`, `docs/internal/archive/architecture/semantic-type-modernization.md`, and `domain/src/strategic_ai_ops.rs`.

Safety baseline: source/provider systems provide evidence, not automatic authority. Customer sends, PMS/CRM/provider writes, schedule/capacity/staffing mutation, money/discount/refund/payment actions, production release, and NVA value claims remain blocked until review and system-of-record proof exist.

## Binding proof chain

```text
source evidence
→ source provenance and data-quality classification
→ validated semantic fact
→ relationship-checked aggregate
→ review/authorization proof
→ legal application action or draft-only recommendation
→ observed outcome and attribution evidence
```

The owner of a later step may consume earlier steps but must not silently re-own them. For example, `app::manager_daily_brief` may rank a staffing review action, but the demand fact remains `domain::analytics::service_demand::Fact`, review gates remain `domain::policy::ReviewGate`, and persisted outcomes remain storage projections until a canonical domain outcome is introduced.

## Maturity key

- L1: concept named.
- L2: concept structurally represented.
- L3: invariants and relationships enforced by types or fallible constructors.
- L4: adopted across domain, application, storage, API, and provider boundaries.

## Canonical concept inventory

| Concept family | Canonical owner/module | IDs | Required fields | Optional fields | Relationships/cardinality | Aggregate boundary | Lifecycle | Provenance | Authorization | Invariants | Traits/conversions/serde | Runtime/storage adoption | Tests | Maturity |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Location/site | `domain::entities::{LocationId, Location}` plus leaf values in `domain::location`; service portfolio in `domain::operations` | `LocationId` | brand, name, timezone, capabilities, policy refs | none currently | Location has many customers, pets by reservation scope, reservations, staff tasks, documents, source facts, and service-line contracts | Core entity aggregate | stable profile with policy-reference changes | source refs on importing snapshots/read-model rows | policy refs drive gates | timezone/name validated; service capabilities are typed `ServiceKind` | serde derives on entity IDs and records; storage uses explicit code/record conversions | storage service-line records and app requests carry `LocationId`; Gingr mapping keeps provider location IDs as source refs | domain/storage/app contract tests | L3, with L4 on core service/storage paths |
| Customer identity/profile | `domain::entities::{CustomerId, Customer}` and `domain::customer::{Name, Email, Phone}`; CRM bridge includes `strategic_ai_ops::crm::StructuredNote` | `CustomerId`; portal IDs live in `domain::portal::CustomerId` | full name, preferred contact | email, mobile phone, portal account | Customer owns zero or many pets; customer participates in reservations, messages, approvals, CRM notes, retention packets | Core entity aggregate; identity matching currently strategic bridge | profile active until superseded by source/system-of-record update | provider customer record via `domain::source::RecordRef` and `source::reservation::Snapshot` | customer-message approval and consent gates before outbound use | contact values trim/non-empty/bounded; no deliverability or consent claim in contact type | nutype validated serde for leaves; total ID wrapper serde | app workflows carry `CustomerId`; storage outcomes cite source refs instead of re-owning profile | safe string/value object and workflow tests | L3 domain, partial L4 |
| Pet identity/profile | `domain::entities::{PetId, Pet, TemperamentProfile, CareProfile}` and `domain::pet::Name` | `PetId` | customer_id, name, species, spay/neuter status, care/temperament defaults | birth date, sex, emergency/vet contacts | Each pet belongs to exactly one customer; may be on many reservations; may have many care notes, vaccine records, temperament observations, incidents | Core pet aggregate | profile evolves by source/staff updates | source reservation snapshot has pet/provider IDs and owner-pet relationship evidence | behavior/medical/vaccine review gates | species, spay/neuter, temperament, care profile are typed; behavior review derived from temperament | serde derives; nutype leaves; provider IDs only promoted through source module | app booking/daily/checkout flows use pet/domain facts | domain policy and app workflow tests | L3 |
| Household / owner-pet relationship | `domain::source::reservation::{Snapshot, OwnerPetRelationship}` now; future household aggregate only if product needs it | source record IDs plus customer/pet IDs after promotion | provenance, relationship evidence | customer/pet/location/service provider IDs | One source snapshot may reference one customer record, one pet record, one location record, one service type; ambiguous owner-pet relationship blocks confident promotion | Source evidence aggregate, not core household yet | resolved or ambiguous per snapshot | full `domain::source::Provenance` | manager/data-quality review when ambiguous | builder requires provenance and relationship; data-quality issues emitted for missing IDs or ambiguity | fallible builder; serde on snapshot | app data-quality workflow and local smoke consume issues/source refs | `domain/tests/source_code_contracts.rs`, app data-quality tests | L3 source, no L4 household aggregate |
| Reservation and service intent | `domain::entities::Reservation`, `domain::entities::reservation`, `domain::reservation`; service catalog in service-line modules and `domain::operations` | `entities::reservation::Id` | location, customer, pet IDs, service, status, starts/ends, source | deposit, add-ons, hard stops | Reservation links one site, one customer, one-or-more pets, one service, optional payment/deposit, many add-ons/hard stops/messages/approvals | Core reservation aggregate | requested → confirmed → checked-in → checked-out/cancelled/rejected etc. | source snapshot and provider reservation refs | manager/medical/behavior/customer-message/deposit gates | typed status/service/add-ons/hard stops; date ordering still needs future hardening | serde derives; storage/app map statuses explicitly | app booking/checkout/retention/manager brief and Gingr endpoints consume | booking/checkout/full-chain tests | L3, partial L4 |
| Service-line contracts | `domain::{boarding, daycare, grooming, training, retail}` with portfolio glue in `domain::operations::service_core` | line-specific IDs and `LocationId` where scoped | contract-specific policies and rules | line-specific optional policies | Service-line contract belongs to a site/core service bundle; service offerings map to `ServiceKind`/service-line types | Per-service domain aggregate | mostly stable policy/config snapshots | source/policy refs via importing record, not always embedded | manager/behavior/medical/payment gates per line | nonzero counts/durations; eligibility decisions are typed | explicit conversions through storage service-line records | storage service_line records cover boarding/daycare/grooming/retail/training; app consumes through workflows | service module and storage contract tests | L3/L4 for core service storage |
| Message and channel | `domain::message` and `domain::entities::Message`; app draft surfaces in workflow modules and tool ports | `MessageId`; body is `message::BodyRef` | subject, direction, channel, status, body ref | approval gate, audit refs | Message belongs to customer/pet/reservation/incident/approval subject; attempts and app drafts must bridge to this record | Core message aggregate for stored/reviewable messages; app owns transient drafts | outbound draft/approval requested/sent/suppressed etc. | source refs and audit refs; body content stored by boundary ref | `policy::ReviewGate::CustomerMessageApproval`; consent is separate and required | `requires_approval_before_send` fails closed for draft/approval states | serde; body ref nutype; no raw body leakage | app daily/retention/booking drafts; API/tool messaging port | app workflow and API DTO tests | L3 domain, L4 draft boundary incomplete |
| Consent and allowed use | Near-term owner should be `domain::message` or new `domain::consent`; current strategic bridge is `strategic_ai_ops::communication::{Channel, ConsentEvidence, Purpose, ConsentStatus}` including `strategic_ai_ops::communication::Channel` | none canonical yet | channel, purpose, status, source | recorded time/subject missing in bridge | Consent is per customer/channel/purpose and must support message/CRM/marketing decisions | Consent evidence aggregate should be separate from message body | granted/missing/opted-out/superseded needed | source system plus future record ref | required before outbound/customer/marketing use | grants only exact channel/purpose; must not be inferred from contact preference | strategic bridge has serde and builders | app CRM retention has its own contact permission; not unified | strategic and CRM workflow tests | L2 strategic, owner decision: migrate to domain consent/message in Phase 1 |
| Staff/labor actor | `domain::entities::{StaffId, ManagerId, ActorRef}` and `domain::staff`; strategic role bridge in `strategic_ai_ops::access`/`labor` | `StaffId`, `ManagerId`, strategic `ActorId` | role/task/status/evidence or actor ref | title, role scope in strategic assistant context | Staff/manager actors request approvals, receive tasks, and provide outcome evidence; labor coverage belongs to demand/capacity facts | Staff task aggregate and actor reference | task statuses and approval decisions | source refs for timeclock/labor facts | manager approval for staffing changes | IDs validated; task role/status typed | nutype serde; strategic bridge separate | app manager brief uses `ActorRef`; storage stores owner persona/action kind | manager brief/storage tests | L3 core, L2 strategic labor bridge |
| Capacity, demand, coverage, shift/skill | Current canonical facts: `domain::analytics::service_demand::Fact`, `domain::daily_brief`, service-line capacity modules; strategic bridge: `strategic_ai_ops::{capacity,labor}`, including `strategic_ai_ops::capacity::OptimizationRecommendation` | analytics IDs; strategic none except site/service/time | location, service/service-line, time bucket/day, demand units, source refs, coverage/labor minutes where strategic | constraints, data-quality issues | Demand fact links to source refs and site/day/service; coverage/shift/skill should link to staff role and time window; recommendation consumes both | Demand fact is analytics aggregate; coverage recommendation needs future domain owner under operations/daily_brief | snapshot/fact/recommendation/outcome | `domain::source::RecordRef` | manager approval before schedule/staffing mutation | nonzero demand/minutes/people; source refs required in app manager brief actions | serde; strategic positive constructors | app manager daily brief consumes analytics demand; no canonical storage for strategic capacity recommendation yet | manager brief workflow/storage tests; strategic tests | L3 demand, L2 coverage/recommendation bridge |
| Knowledge document and citation | Existing owner: `domain::entities::Document` and `domain::document`; strategic assistant bridge: `strategic_ai_ops::knowledge`/`assistant` | `DocumentId`; strategic `DocumentId` bridge | classification/source/status/storage/original file for core docs; applicability/citation for strategic | review due, service/role scope | Document can apply to locations, services, roles; citations refer to document+section; answers require citations/escalation | Core document aggregate; assistant packet is app/strategic bridge | received/extracting/awaiting review/approved/stale/superseded | storage ref, audit refs, future source refs | medical/document review, retrieval role scope | `requires_human_review_before_use`; strategic applies only when approved and scoped | serde; file/storage values validated; strategic bridge serde | app tools document/OCR port; API vaccine document workflow | app/API document tests and strategic assistant test | L3 document, L2 assistant bridge |
| Finance and money | Canonical owner: `domain::money::{Money, MinorUnits, Currency}` and `domain::payment`; strategic bridge: `strategic_ai_ops::financial`, including `strategic_ai_ops::financial::MoneyCents` | payment `Reference`; strategic none | amount/currency for domain money; period/service/source/revenue/costs for strategic fact | payment/deposit refs, recommendation | Money is scalar; financial facts link site/service/period/source; recommendations must link reviewed action/outcome | Money/payment domain aggregate; financial insights need new owner under analytics/finance if kept | payment/deposit statuses; facts by reporting period | source record refs should be added; strategic only stores source enum now | manager approval and refund/deposit exception gates | non-negative cents; net revenue can currently panic if discounts/refunds exceed gross in strategic bridge, known future hardening | domain money checked constructors; strategic custom serde for nonnegative cents | storage outcome records and app payment ports; no L4 strategic finance slice | payment/money tests; strategic tests | L3 money/payment, L2 strategic finance |
| Source/provenance/data quality | `domain::source` and `domain::data_quality` | source `record::Id`; extraction batch; data-quality field paths | system, endpoint, record, batch, pulled_at, request scope, schema version, payload hash, raw ref | related record IDs; issues/warnings | Source record may relate to customer/pet/location/service/staff/payment/etc.; issues point to provenance and field path | Source evidence aggregate | observed/imported/promoted/superseded via source and issue status | primary owner; everything else cites it | data-quality workflow/manager review | builder requires provenance; source snapshots produce issues for missing or ambiguous relationships | fallible constructors; validated serde on selected IDs; explicit provider-to-source conversions | app data-quality, storage read models, Gingr mapping | source, data-quality, storage tests | L3/L4 for data-quality slice |
| Review/authorization proof | `domain::policy::{ReviewGate, automation}` and `domain::entities::approval::Record`; app modules expose safe/blocked action enums | `approval::Id`, policy IDs | target, gate, lifecycle, requested_by/at | audit refs | Approval targets reservation/document/vaccine/incident/message; app recommendations must point at review gates | Approval aggregate | requested/approved/rejected/cancelled/superseded | audit refs and source refs from packet | canonical authorization boundary | `is_applicable`, `is_terminal_decision`, decision actor/time | serde; no opaque booleans | app workflows and storage review queue codes consume | policy, app, storage tests | L3, partial L4 |
| Legal application action | App/workflow owners: `domain::workflow::{AllowedAction, RecommendedAction, Result}` plus app module-specific `SafeAgentAction`/`BlockedAction`; no live mutation owner yet | `workflow::EventId`, app action IDs | subject, actor, location, allowed actions, review gates, recommendation/action | structured output, risk flags | Every legal action must be derived from reviewed packet and target entity, then executed by shell/provider path only after authorization | Workflow result/action aggregate; app packet aggregate | draft/review/approved/executed/outcome (partially represented) | source refs and verification notes | review gates and approval records | blocked action enums encode no-go actions; live execution absent | serde; app-local nutypes | API/worker shells expose safe local workflows only | app/API/spacetimedb tests | L3 for draft/review actions, L1/L2 for legal execution |
| Outcome/attribution | Current app outcome owners: `app::manager_daily_brief::OutcomeRecord`, `app::data_quality_hygiene::OutcomeRecord`, `app::crm_retention::OutcomeRecord`; storage projections; strategic bridge `strategic_ai_ops::outcome::Record` | app action IDs; strategic outcome `Id` | action/workstream, metric, before/after/actual, source refs, recorded_by/at | attribution strength, feedback | Outcome links reviewed action to measured result and source refs; reviewed action attribution strengthens evidence but does not issue value-claim authority | App outcome aggregate by workflow; future canonical `domain::outcome` likely | completed/deferred/suppressed/source-wrong | source refs required in app records; strategic only source enum | human/system-of-record disposition | manager brief fails closed unless action and all source evidence cited | serde; storage JSON codecs | manager/data-quality storage outcome records | app/storage outcome tests and strategic test | L3 per app slice, L2 strategic generalized outcome |

## Required relationship matrix

| Relationship | Canonical owner | Required proof | Cardinality and gate | Current implementation | Gaps / migration target |
| --- | --- | --- | --- | --- | --- |
| lead ↔ customer/pet/site/service | `domain::source::reservation::Snapshot` for source links; `domain::entities::Reservation` for validated relationship; `domain::lead`/future lead-response aggregate for lead-specific events | source provenance, related provider IDs, identity match, data-quality status | one lead event should bind to one site and intended service; customer/pet may be none/candidate/ambiguous until review | `strategic_ai_ops::lead_response::Event` carries location/service/customer match; source snapshot carries customer/pet/location/service record IDs | migrate lead event IDs/kind/SLA to canonical lead/workflow module and bridge identity through `domain::source` |
| event ↔ SLA ↔ attempt ↔ message ↔ conversion | future `domain::lead_response` or `domain::lead`; `domain::entities::Message` owns stored message; `domain::workflow` owns event/recommendation; outcome owner records conversion | event provenance, SLA target/due status, attempt review gate, message approval, conversion attribution with source refs | one event can have one SLA and many attempts; outbound attempt must create/refer to a draft message; zero-or-one conversion attribution until outcome | strategic bridge has `Event`, `ResponseSla`, `ContactAttempt`, `ResponsePacket`, `ConversionAttribution` | add message ID linkage and source refs; replace bridge with owner module in Phase 1/2 |
| note ↔ evidence/visibility/consent | `domain::entities::CareNote` for care/staff notes; future CRM note owner under `domain::customer` or `domain::crm`; consent owner to be created under message/consent | source/audit refs, visibility, allowed use, consent evidence | one note targets customer/pet/reservation/incident; marketing use requires explicit consent/allowed use | strategic `crm::StructuredNote` covers visibility/allowed use/review state; app CRM retention has contact permission | migrate structured CRM note and consent evidence to canonical domain owner; bridge app CRM permission to it |
| demand ↔ coverage ↔ shift/skill | `domain::analytics::service_demand::Fact` for demand; future operations/labor owner for coverage; `domain::staff` for role/task; app manager brief for recommendation packet | source refs for demand/coverage, location/day/service, role/skill, time bucket, manager gate | many demand facts per site/day/service; coverage per role/window; recommendation consumes one-or-more demand and coverage records | strategic `DemandUnit`, `ScheduledCoverage`, `OptimizationRecommendation`; app manager brief ranks demand facts | define canonical labor coverage/skill/shift facts; bridge/delete strategic labor types |
| document ↔ applicability ↔ citation | `domain::entities::Document`/`domain::document` for document evidence; future assistant/knowledge module for applicability/citation | verified document/storage ref, approval/freshness status, scope by location/service/role, citation section | document can apply to many sites/services/roles; answer must cite one-or-more approved applicable documents or escalate | strategic `knowledge::Document`, `Applicability`, `Citation`, `assistant::AnswerPacket` | rehome applicability/citation to domain knowledge/assistant or app tool port; link to core `DocumentId` |
| recommendation ↔ reviewed action ↔ outcome | `domain::workflow::RecommendedAction` and `domain::entities::approval::Record`; app workflow action IDs; future `domain::outcome` | review gate/approval proof, source evidence, action ID, outcome source refs and attribution | recommendation can produce zero-or-many reviewed actions; outcome must cite reviewed action and source evidence before value claim | manager brief `BriefAction` + `OutcomeRecord::labor_savings_claim_for_action`; strategic `outcome::Record` generic bridge | generalized outcome owner needed after vertical slices prove common shape |

## `strategic_ai_ops.rs` overlap classification

| Strategic type/module | Existing owner | Decision | Reason / bridge rule |
| --- | --- | --- | --- |
| `strategic_ai_ops::source::System` | `domain::source::System` | Delete after migration | It duplicates source-system authority. Add missing variants to `domain::source::System` only when source contracts require them, then convert strategic tests. |
| `strategic_ai_ops::time::Window` | none broad; app/analytics currently use day/date, service modules use local windows | Migrate | Keep as canonical `domain::time` or owner-specific window when cross-service SLA/labor windows are implemented. Current bridge is acceptable only for strategic tests. |
| `strategic_ai_ops::access::{ActorId, Title, ActorRole, VisibilityScope, AllowedUse}` | `domain::entities::ActorRef`, `domain::staff`, `domain::policy`; no canonical visibility/allowed-use module | Migrate | Actor identity should not duplicate `StaffId`/`ManagerId`; visibility/allowed-use should become a domain authorization/consent vocabulary used by CRM, assistant, and message modules. |
| `strategic_ai_ops::identity::{Confidence, Match}` | `domain::source::reservation::OwnerPetRelationship` for owner/pet matching; no general identity match owner | Bridge then migrate | Use as lead/CRM bridge until a `domain::identity` or source promotion result exists. |
| `strategic_ai_ops::communication::{Channel, Purpose, ConsentStatus, ConsentEvidence}` | `domain::message::Channel` and app CRM contact permission | Migrate | Channel duplicates message channel. Consent/purpose need canonical `domain::consent` or `domain::message::consent` owner. |
| `strategic_ai_ops::lead_response::{EventId, EventKind, Event, ResponseSla, ContactAttempt, ConversionAttribution, ResponsePacket}` | `domain::lead`, `domain::workflow`, `domain::entities::Message`, app retention/booking outcomes | Migrate | This is a valuable missing aggregate, but owner should be a canonical lead-response/lead module, not the catch-all. Bridge to `workflow::Event`, `Message`, and outcome records. |
| `strategic_ai_ops::crm::{NoteId, NoteBody, StructuredNote, SegmentMembership}` | `domain::entities::CareNote`, `domain::customer`, app `crm_retention` | Migrate | Customer intelligence notes are not care notes; create a canonical CRM/customer-intelligence module with consent/visibility. Do not let app CRM retention own canonical customer profile facts. |
| `strategic_ai_ops::labor::{Minutes, SignedMinutes, PeopleCount, Role, ScheduledCoverage}` | `domain::staff`, `domain::daily_brief`, `domain::analytics::service_demand` | Migrate | Labor coverage is a real missing concept. Move to operations/labor module with source refs, shift/skill and manager gate. |
| `strategic_ai_ops::capacity::{DemandUnit, OptimizationRecommendation}` | `domain::analytics::service_demand::Fact`, app `manager_daily_brief::BriefAction` | Bridge then migrate | Demand overlaps analytics; recommendation overlaps app brief. Canonical owner should distinguish source fact from reviewable recommendation. |
| `strategic_ai_ops::knowledge::{DocumentId, Document, Applicability, Citation}` | `domain::entities::Document`, `domain::document`, app tool document port | Migrate | Do not duplicate document IDs. Applicability/citation belong in canonical knowledge/assistant layer that references `entities::DocumentId`. |
| `strategic_ai_ops::assistant::{ActorContext, AnswerPacket}` | `domain::agent`, app `agents`, app tool ports | Bridge | Assistant packets are application/use-case envelopes unless/until retrieval authorization becomes domain policy. Keep citations/domain docs canonical. |
| `strategic_ai_ops::financial::{MoneyCents, SitePeriod, RevenueFact, Insight}` | `domain::money`, `domain::payment`, `domain::analytics`, storage outcome records | Migrate | MoneyCents duplicates `domain::money`; revenue fact/insight need canonical finance/analytics owner with source refs. Known gap: net revenue should be checked, not panicking. |
| `strategic_ai_ops::outcome::Record` | app outcome records and storage projections; future `domain::outcome` | Bridge then migrate | Generic outcome is needed, but must be proven by app slices first. Generalize only after manager/data-quality/lead slices converge. |

## Serde and boundary rules

- Domain/app values that deserialize from stored packets must validate on deserialize; builder-required fields are not sufficient validation.
- Provider DTOs and storage records may preserve provider/storage wire vocabulary, but promotion into domain facts must be explicit and fallible when validation, normalization, authorization, or trust changes.
- `From` is allowed only for total, lossless, authority-neutral conversion; use `TryFrom` or named promotion for source/provider/storage/domain crossing.
- Sensitive note/body/message text must avoid casual `Debug`/`Display` leakage and should expose disclosure only through approved renderers or review packets.

## Runtime/storage adoption snapshot

- L4-ish adopted slices today: service-line contracts through `domain::operations` ↔ `storage::service_line`, data-quality hygiene source/read-model/outcome path, manager daily brief app/storage/API proof, and local full-chain smoke draft workflow.
- Strategic AI Ops is currently tested only in `domain/tests/strategic_ai_ops_model_contracts.rs`; it is an exploratory contract bridge, not a canonical owner.
- Gingr integration owns provider endpoints, DTOs, transport, webhook parsing, and mapping. It must keep provider IDs and quirks at the boundary until promoted into `domain::source` or canonical domain values.

## Final independent review update — 2026-08-13

Final review artifact: `docs/internal/reviews/semantic-domain-final.md`.

Verdict: `approved=true` after closeout remediation. The strategic vertical slices prove meaningful L3 behavior and partial L4 adoption, and canonical owner modules now own their definitions directly. `domain/src/strategic_ai_ops_bridge.rs` is a deprecated compatibility facade that re-exports from `domain::access`, `domain::consent`, `domain::identity`, `domain::lead::response`, `domain::customer::intelligence`, `domain::operations::{labor,capacity,time_bucket}`, `domain::agent::{assistant,knowledge}`, and `domain::analytics::{finance,outcome}` rather than serving as the implementation owner.

| Strategic workstream | Final review rating | Evidence paths | Required remediation before green |
| --- | --- | --- | --- |
| Lead response source to reviewed queue-only action | L3, partial L4 adoption; ownership resolved | `domain/src/lead.rs`, `domain/tests/strategic_ai_ops_model_contracts.rs`, `apps/api/src/http.rs`, `apps/api/tests/api_dto_contracts.rs` | Definitions now live under `domain::lead::response`; compatibility facade re-exports from the owner. |
| CRM retention and customer intelligence | L3 app/domain behavior, partial L4 through manager brief/storage; ownership resolved | `domain/src/customer.rs`, `app/src/crm_retention.rs`, `app/src/manager_daily_brief.rs`, `storage/src/operations.rs` | Definitions now live under `domain::customer::intelligence`. |
| Capacity/labor to Manager Daily Brief | L3/L4 slice behavior; ownership resolved | `domain/src/operations.rs`, `app/src/manager_daily_brief.rs`, `storage/tests/manager_daily_brief_outcome_storage.rs` | Labor/capacity/time-bucket definitions now live under canonical operations modules. |
| Permissioned knowledge retrieval to assistant packet | L3 app/domain behavior, partial L4 API exposure; ownership resolved | `domain/src/agent.rs`, `app/src/permissioned_knowledge.rs`, `apps/api/src/http.rs` | Assistant/knowledge definitions now live under canonical agent modules. |
| Site finance recommendation and reviewed outcome | L3 app/domain behavior, partial L4 API/storage proof; ownership resolved | `domain/src/analytics.rs`, `app/src/site_finance.rs`, `storage/src/operations.rs` | Finance/outcome definitions now live under canonical analytics modules. |
| Approval/outbox/read-model infrastructure | L3 storage infrastructure, partial L4 across proven slices | `storage/src/operations.rs`, `storage/tests/approval_outbox_infrastructure.rs`, `apps/api/src/http.rs` | No live provider-write authority; keep it as review/outbox infrastructure only. |
| Source/provenance/data-quality spine | L3/L4 for source and data-quality slices | `domain/src/source.rs`, `domain/tests/source_code_contracts.rs`, app/storage/API data-quality tests | Preserve as source authority while moving strategic bridge concepts. |

Closeout GREEN proof: `domain/tests/semantic_rehome_contracts.rs::canonical_semantic_owner_modules_do_not_hide_renamed_bridge_reexports` now passes, alongside the direct-bridge ownership test and the canonical-owner public-surface test.

## Residual owner decisions safely defaulted

No human product-owner decision is blocking this atlas. Defaults used here:

1. Create/migrate toward canonical domain owners instead of keeping `strategic_ai_ops` permanent.
2. Treat consent/visibility/allowed-use as shared authorization vocabulary, not app-local booleans.
3. Treat generic outcomes as a future domain module only after at least one more vertical slice proves common shape.
4. Keep live execution out of scope; legal application actions remain reviewed/draft artifacts until a separate side-effect authorization card creates capabilities.
